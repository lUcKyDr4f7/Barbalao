/* import { useState, createContext } from 'react';

export const StdImgCtx = createContext();
export const SetStdImgCtx = createContext();

export default function StdImgProvider({stdImg, children}) {

    const [stdImg, setStdImg] = useState(false);

    return (
        <StdImgCtx.Provider value={[stdImg, setStdImg]}>
            {children}
        </StdImgCtx.Provider>
    )
} */